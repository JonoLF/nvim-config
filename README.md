# Neovim config
Cloning the repo:
```
cd ~/.config/nvim/
gh repo clone JonoLF/nvim-config
```
Setup steps:
```
./nvim_install &&\
./setup
```
> Note: requires the [GITHUB CLI](https://cli.github.com/)
